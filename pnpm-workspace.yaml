overrides:
  # @better-auth/kysely-adapter 1.6.13 imports DEFAULT_MIGRATION_TABLE, dropped
  # in kysely 0.29. Pin to 0.28.17 (we use the Drizzle adapter anyway).
  kysely: 0.28.17

allowBuilds:
  esbuild: true
  msw: false
  sharp: true
  unrs-resolver: true
